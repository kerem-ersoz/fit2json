"""FitSift web layer — a FastAPI JSON API wrapping the fit2json library.

Local, single-user. Reuses the existing parser / output / memory / analyzer modules
and serves the built React SPA. See ``fit2json serve``.
"""

BRAND_NAME = "FitSift"
BRAND_TAGLINE = "Sift your workouts into insight"
